# 快速开始（部署指南）

> 雪哨（xueqiu-watch）使用者的极简部署手册。完整使用说明见仓库根目录 `README.md`。

## 0. 一句话结论

运行雪哨**只需要一个 Chromium 浏览器 + 开发者模式加载（或未来的商店安装）+ 已登录雪球**。**不需要 WorkBuddy，不需要 Node、Python 等任何开发环境，不需要本地编译。**

## 1. 前置条件

- ✅ **Chromium 内核浏览器**：Chrome / Edge / Brave / Arc 等，版本支持 Manifest V3。
- ✅ **一个已登录 `xueqiu.com` 的浏览器标签页**（且保持打开）。
- ✅ 你对雪球确有「特别关注」分组（可选企微推送时才需要企业微信账号）。
- ❌ **不需要**：WorkBuddy、git、Node.js、Python、任何服务器。

## 2. 方式一：开发者模式加载已解压（推荐普通用户 / 快速体验）

1. 下载仓库 Release 中的 `xueqiu-watch-v1.3.0.zip`，解压到一个固定目录（如 `D:\xueqiu-watch`）。
2. 浏览器地址栏输入 `chrome://extensions`（Edge 为 `edge://extensions`）。
3. 右上角打开 **开发者模式（Developer mode）**。
4. 点 **「加载已解压的扩展程序」**，选择刚才的解压目录。
5. 右上角出现「雪哨」图标即成功；Chrome 重启自动启用，更新时点「刷新」。

## 3. 方式二：Chrome 网上应用店安装（占位，本期暂未上架）

1. 商店搜索「雪哨」或访问 Release 中提供的商店链接。
2. 点「添加至 Chrome」，按提示授权权限即可。

> ⚠️ 上架状态见 `STORE_GUIDE.md`；**本期（v1.3.0）暂未上架**，仅用方式一开发者模式分发。

## 4. 雪球登录态要求

- 必须已登录 `xueqiu.com`；扩展借用该登录态调 API，不读密码、不存 Cookie。
- 保持至少一个雪球标签页打开，否则收不到提醒（MV3 硬限制）。

## 5. 可选：企业微信推送（推到个人微信）

三步概览（详情见 `README.md`「企业微信推送配置」）：

1. 企业微信后台（个人微信扫码即可建企业）自建一个应用，拿到 CorpID / Secret / AgentID，并开启「在微信中接收」。
2. 扩展设置页「④ 企业微信推送」勾选启用并填入上述凭证。
3. 点「📤 测试企微推送」，微信收到测试消息即配置成功。

## 6. 验证安装是否成功

- 打开扩展设置页 → 点「📢 发送测试提醒」。
- 预期：右下角系统通知 + 右侧磁吸小窗同时出现（声音需在设置开启）。
- 失败则回看 `README.md`「常见问题 / 故障排查」或下方排错小节。

### 排错小程序

| 现象 | 排查 |
|---|---|
| 测试提醒无反应 | 确认已开开发者模式、扩展已加载、Chrome 未完全静音 |
| 检测登录失败 | 打开 xueqiu.com 登录并保持标签页，再点设置页「🔧 一键诊断」 |
| 收不到真实新帖 | 点扩展图标看「监控博主：N 人」，N=0 说明没拉到特别关注分组 |

## 附：小白用户最短路径（30 秒）

下载 zip → 解压 → `chrome://extensions` → 开开发者模式 → 加载已解压 → 打开 `xueqiu.com` 登录 → 点测试提醒。

## 附：开发者指引（仓库协作 / 改代码）

- 源码即仓库根目录；改完回到 `chrome://extensions` 点「刷新」即可热更。
- **测试**：`node test_harness.js`（53 条断言）本地校验逻辑。
- **发布**：按 `RELEASE.md` 流程打包 zip + 用 GitHub Contents / Releases API 建 Release（**非 `git push`**）。
- 详情见 `HANDOFF.md`、`STORE_GUIDE.md`、`docs/ARCHITECTURE.md`。
